import unittest


# Placeholder test so that CI does not fail with "no tests ran".
# Replace with real tests as the codebase grows.
class SmokeTestCase(unittest.TestCase):
    def test_dummy(self):
        self.assertTrue(True)

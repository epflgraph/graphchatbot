The examples below are from other courses, and show only how to split, order and bound a
list of points. Never carry their subject matter into your own output. Each `<output>` is a
complete list for its topic, not an extract; each `<rejected_points>` entry is a point that
was considered and left out, with the reason it was.

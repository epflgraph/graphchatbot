Using the provided <instructions>, <sources>, and <dialog_history>, produce the practice material.

**Write every field (`title`, `subtitle`, `questions`, `options`, `explanations`, and `link_response`) in {% if lang_code %}{{ languages[lang_code] }}{% else %}the language the student is writing in{% endif %}.** These instructions are in English; that says nothing about which language to write the material in.
